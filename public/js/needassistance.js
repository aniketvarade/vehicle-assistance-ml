function assistance() {
    var total = document.getElementById("total").nodeValue;                         // total stores the total kms travelled by vehicle
    window.location.href = "result.html";                                           // function is used to store values entered by user in database and move to next page
}